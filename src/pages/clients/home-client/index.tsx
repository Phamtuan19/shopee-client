import { Button, HeaderClient } from '@components';

export const HomeClient = () => {
   return (
      <div>
         <HeaderClient />

         <Button variant="container" size="large">
            Hello
         </Button>
      </div>
   );
};
